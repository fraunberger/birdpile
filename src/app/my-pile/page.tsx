import { MyPile } from "@/components/my-pile/MyPile";

export const metadata = {
  title: "My Pile — BirdFinds",
  description: "Your saved items — want to check out.",
};

export default function MyPilePage() {
  return <MyPile />;
}
